spring.application.name=prod-ready-features

spring.datasource.url =jdbc:mysql://localhost:3306/prod-ready-features?useSSl=false
spring.datasource.username =root
spring.datasource.password =Sahil@9192

spring.jpa.hibernate.ddl-auto =create
spring.jpa.show-sql =true
spring.jpa.properties.hibernate.format_sql =true

#DevTool Configuration
#spring.devtools.restart.enabled =true
#spring.devtools.restart.exclude =com/sahil/prod_ready_features/prod_ready_features/dtos/**

# How often (in milliseconds) DevTools checks the classpath for file changes, by default it is 1000 ms.
#spring.devtools.restart.pollInterval = 2000

# Minimum amount of time (in milliseconds) a file must remain unchanged before DevTools triggers a restart, by default it is 400 ms .
#spring.devtools.restart.quietPeriod = 10